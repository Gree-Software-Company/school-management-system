interface createStudentI {
  firstName: string; // Student's first name
  lastName: string; // Student's last name
  classId: number; // Unique identifier for the class
  gender: string;
  amount?: number;
  semesterId: number;
}
